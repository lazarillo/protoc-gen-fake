[env]
# Fixes LNK2038 mismatch on Windows (MSVC) by forcing CMake dependencies (like protobuf-src)
# to use the dynamic CRT (/MD or /MDd) matching the abseil dependency.
CMAKE_MSVC_RUNTIME_LIBRARY = "MultiThreadedDLL$<$<CONFIG:Debug>:Debug>"
